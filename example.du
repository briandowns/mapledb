#!/usr/bin/env dictu

import System;

from "db.du" import DB;


{ // 
    const db = DB();

    db.set("first_name", "Brian");
    db.set("last_name", "Downs");
    db.set("age", 43);

    db.get("first_name");

    print(db.del("first_name"));

    System.exit(0);
}