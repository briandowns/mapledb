

import IO;
import Process;
import System;

from UnitTest import UnitTest;

from "../db.du" import DB;


class TestNewDB < UnitTest {
    tearDown() {
        
    }

    testNewMemoryDB() {
        const db = DB();
        this.assertNotNil(db);
    }

    testNewFileDB() {
        const db = DB("unit_tests.db");
        this.assertNotNil(db);
    }
}

class TestSetValue < UnitTest {
    testSetValue() {

    }
}

class TestGetValue < UnitTest {
    testGetValue() {
        
    }
}

TestNewDB().run();
TestSetValue().run();
TestGetValue().run();
